[package]
name = "signalmap-edge-core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Allocation-free SignalMap edge primitives for tiny no_std devices"

[lib]
name = "signalmap_edge_core"
path = "src/lib.rs"

[features]
default = []
std = []
